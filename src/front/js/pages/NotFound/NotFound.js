import React, { useState } from "react";

import "./styles.scss";

const NotFound = () => (
	<div className="not-found">
		<h1>Lo sentimos.</h1>
		<h2>Ésta página no existe.</h2>
	</div>
);

export default NotFound;
